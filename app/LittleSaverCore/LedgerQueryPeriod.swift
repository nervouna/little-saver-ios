/// Domain periods deliberately independent of generated Siri intent types.
public enum LedgerTimePeriod { case unknown, day, week, month, year }
public enum LedgerInsightsPeriod { case unknown, week, month, year }
